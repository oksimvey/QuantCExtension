import*as vscode from"vscode";import{BUILTINS,KEYWORDS,TYPES}from"../lexer/Keywords";import{LanguageService}from"./LanguageService";export class HoverProvider implements vscode.HoverProvider{constructor(private s:LanguageService){}provideHover(d:vscode.TextDocument,p:vscode.Position){const r=d.getWordRangeAtPosition(p);if(!r)return;const w=d.getText(r),md=new vscode.MarkdownString();if(KEYWORDS.has(w)){md.appendCodeblock(w,"qc");md.appendMarkdown("\nQuantC keyword.");return new vscode.Hover(md,r)}if(TYPES.has(w)){md.appendCodeblock(w,"qc");md.appendMarkdown("\nBuilt-in QuantC type.");return new vscode.Hover(md,r)}const b=BUILTINS.get(w);if(b){md.appendCodeblock(b.signature,"qc");md.appendMarkdown("\n"+b.documentation);return new vscode.Hover(md,r)}const c=this.s.project.getClass(w);if(c){md.appendCodeblock("class "+c.name+(c.baseClass?" extends "+c.baseClass:""),"qc");return new vscode.Hover(md,r)}return}}
