import*as vscode from"vscode";export class CodeActionProvider implements vscode.CodeActionProvider{provideCodeActions(d:vscode.TextDocument,_r:vscode.Range,c:vscode.CodeActionContext){const out:vscode.CodeAction[]=[];for(const x of c.diagnostics){if(x.message.includes("must be initialized")){const a=new vscode.CodeAction("Initialize with a default value",vscode.CodeActionKind.QuickFix);a.diagnostics=[x];a.edit=new vscode.WorkspaceEdit();a.edit.insert(d.uri,x.range.end," = 0");out.push(a)}if(x.message.startsWith("Unknown type")){const n=x.message.match(/'([^']+)'/)?.[1];if(n){const a=new vscode.CodeAction("Create class '"+n+"'",vscode.CodeActionKind.QuickFix);a.diagnostics=[x];a.edit=new vscode.WorkspaceEdit();a.edit.insert(d.uri,new vscode.Position(d.lineCount,0),"\nclass "+n+" {\n}\n");out.push(a)}}}return out}}
