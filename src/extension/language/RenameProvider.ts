import*as vscode from"vscode";import{LanguageService}from"./LanguageService";export class RenameProvider implements vscode.RenameProvider{constructor(private s:LanguageService){}prepareRename(d:vscode.TextDocument,p:vscode.Position){return d.getWordRangeAtPosition(p)}provideRenameEdits(d:vscode.TextDocument,p:vscode.Position,n:string){const r=d.getWordRangeAtPosition(p);if(!r)return;const w=d.getText(r),e=w.replace(/[.*+?^$()|[\]{}\\]/g,"\\$&"),edit=new vscode.WorkspaceEdit();for(const f of this.s.project.allFiles()){const re=new RegExp("\\b"+e+"\\b","g");let m:RegExpExecArray|null;while((m=re.exec(f.text))){const line=f.text.slice(0,m.index).split("\n").length-1,col=m.index-(f.text.lastIndexOf("\n",m.index-1)+1);edit.replace(vscode.Uri.parse(f.uri),new vscode.Range(line,col,line,col+w.length),n)}}return edit}}
